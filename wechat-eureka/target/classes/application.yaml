server:
  port: 10086
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:10086/eureka
spring:
  profiles:
    active: ${profile}
